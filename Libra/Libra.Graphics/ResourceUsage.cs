#region Using

using System;

#endregion

namespace Libra.Graphics
{
    public enum ResourceUsage
    {
        Default     = 0,
        Immutable   = 1,
        Dynamic     = 2,
        Staging     = 3,
    }
}
