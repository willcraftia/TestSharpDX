#region Using

using System;

#endregion

namespace Libra.Graphics
{
    public enum TextureAddressMode
    {
        Wrap        = 1,
        Mirror      = 2,
        Clamp       = 3,
        Border      = 4,
        MirrorOnce  = 5,
    }
}
