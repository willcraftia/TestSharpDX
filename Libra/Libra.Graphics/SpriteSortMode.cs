#region Using

using System;

#endregion

namespace Libra.Graphics
{
    public enum SpriteSortMode
    {
        Deferred    = 0,
        Immediate   = 1,
        Texture     = 2,
        BackToFront = 3,
        FrontToBack = 4
    }
}
